import Layout from '@theme/Layout';
import type {ReactNode} from 'react';

export default function Home(): ReactNode {
  return (
    <Layout title="Ragnarok Offline Player Guide">
      <main aria-label="Ragnarok Offline Player Guide" />
    </Layout>
  );
}
